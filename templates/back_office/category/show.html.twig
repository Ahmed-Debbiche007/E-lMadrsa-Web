{% extends 'back_office/backbase.html.twig' %}

{% block title %}Category{% endblock %}



{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            
            
            <tr>
                
                <td><img src="{{ asset('uploads/images/' ~ category.getCategoryimage()) }}" alt="taswira" width="200" height="100" /></td>
            </tr>
            <tr>
                
                <td>{{ category.categoryname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}">back to list</a>

    <a href="{{ path('app_category_edit', {'categoryid': category.categoryid}) }}">edit</a>

    {{ include('back_office/category/_delete_form.html.twig') }}
{% endblock %}
