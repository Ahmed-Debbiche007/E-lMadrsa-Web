{% extends 'back_office/backbase.html.twig' %}

{% block title %}Badge{% endblock %}

{% block body %}
    <h1>Badge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Badgeid</th>
                <td>{{ badge.badgeid }}</td>
            </tr>
            <tr>
                <th>Badgetype</th>
                <td>{{ badge.badgetype }}</td>
            </tr>
            <tr>
                <th>Badgeimage</th>
                <td>{{ badge.badgeimage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_badge_index') }}">back to list</a>

    <a href="{{ path('app_badge_editt', {'badgeid': badge.badgeid}) }}">edit</a>

    {{ include('badge/_delete_form.html.twig') }}
{% endblock %}
