<form method="post" action="{{ path('app_post_delete', {'postid': post.postid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.postid) }}">
    <button class="badge bg-danger">Delete</button>
</form>
