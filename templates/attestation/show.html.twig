{% extends 'base.html.twig' %}

{% block title %}Attestation{% endblock %}

{% block body %}
    <h1>Attestation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idattestation</th>
                <td>{{ attestation.idattestation }}</td>
            </tr>
            <tr>
                <th>Idparticipation</th>
                <td>{{ attestation.idparticipation }}</td>
            </tr>
            <tr>
                <th>dateAcq </th>
                <td>{{ attestation.dateAcq? attestation.dateAcq|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_attestation_index') }}">back to list</a>

    <a href="{{ path('app_attestation_edit', {'idattestation': attestation.idattestation}) }}">edit</a>

    {{ include('attestation/_delete_form.html.twig') }}
{% endblock %}
