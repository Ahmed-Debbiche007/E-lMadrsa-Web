<form method="post" action="{{ path('app_chatsessions_delete', {'idsession': chatsession.idsession}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chatsession.idsession) }}">
    <button class="btn">Delete</button>
</form>
