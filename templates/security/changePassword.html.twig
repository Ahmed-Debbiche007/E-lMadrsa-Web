<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login
    </title>
    <link rel="stylesheet" href="{{ asset('back/assets/css/main/app.css')}}">
    <link rel="stylesheet" href="{{ asset('back/assets/css/pages/auth.css')}}">
    <link rel="shortcut icon" href="{{ asset('back/assets/images/logo/favicon.svg')}}" type="image/x-icon">
    <link rel="shortcut icon" href="{{ asset('back/assets/images/logo/favicon.png')}}" type="image/png">
</head>

<body>
<div id="auth">

    <div class="row h-100">
        <div class="col-lg-5 col-12">
            <div id="auth-left">
                <div class="auth-logo">
                    <a href="index.html">
                        <h1 class="auth-title">E-lMadrsa</h1>
                    </a>
                </div>
                <h1>Change Password.</h1>
                <p class="auth-subtitle mb-5">Recover your account and resume your journey {{ user.getId() }}</p>

                <form method="post" action="{{ path('app_change') }}">
                    <div class="form-group position-relative has-icon-left mb-4">
                        <input class="form-control form-control-xl" placeholder="Password" type="password" name="password" id="inputPassword" autocomplete="current-password" required>
                        <div class="form-control-icon">
                            <i class="bi bi-shield-lock"></i>
                        </div>
                    </div>
                    <input class="form-control form-control-xl" placeholder="Confirm Password" type="hidden" value="{{ user.getId() }}" name="user" id="user" autocomplete="current-password" required>
                    <div class="form-group position-relative has-icon-left mb-4">
                        <input class="form-control form-control-xl" placeholder="Confirm Password" type="password" name="confirmPassword" id="inputPassword" autocomplete="current-password" required>
                        <div class="form-control-icon">
                            <i class="bi bi-shield-lock"></i>
                        </div>
                    </div>

                    <button class="btn btn-primary btn-block btn-lg shadow-lg mt-5" type="submit">Change</button>
                </form>

            </div>
        </div>
        <div class="col-lg-7 d-none d-lg-block">
            <div id="auth-right"></div>
        </div>
    </div>

</div>
</body>

</html>