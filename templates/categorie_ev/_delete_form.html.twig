<form method="post" action="{{ path('app_categorie_ev_delete', {'id': categorie_ev.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_ev.id) }}">
    <button class="badge bg-danger">Delete</button>
</form>
