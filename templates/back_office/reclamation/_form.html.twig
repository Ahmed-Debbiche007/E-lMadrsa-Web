
{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}

<table>
    <tr>
        <div class="form-group position-relative has-icon-left mb-4">
            {{ form_widget(form.sujet, { attr: { class: 'form-control form-control-xl', placeholder: 'sujet' } }) }}
            <td> {{ form_errors(form.sujet) }}</td>

        </div>
    </tr>
    <tr>
        <div class="form-group position-relative has-icon-left mb-4">
            {{ form_widget(form.description, { attr: { class: 'form-control form-control-xl', placeholder: 'description' } }) }}
            <td> {{ form_errors(form.description) }}</td>

        </div>
    </tr>

    <tr>
        <div class="form-group position-relative has-icon-left mb-4">
            {{ form_widget(form.iduser, { attr: { class: 'form-control form-control-xl', placeholder: 'user' } }) }}
            <td> {{ form_errors(form.iduser) }}</td>

        </div>
    </tr>

</table>
<button class="btn btn-primary btn-block btn-lg shadow-lg mt-5">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}