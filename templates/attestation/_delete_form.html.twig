<form method="post" action="{{ path('app_attestation_delete', {'idattestation': attestation.idattestation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attestation.idattestation) }}">
    <button class="btn">Delete</button>
</form>
