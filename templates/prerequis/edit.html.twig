{% extends 'base.html.twig' %}

{% block title %}Edit Prerequis{% endblock %}

{% block body %}
    <h1>Edit Prerequis</h1>

    {{ include('prerequis/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_prerequis_index') }}">back to list</a>

    {{ include('prerequis/_delete_form.html.twig') }}
{% endblock %}
