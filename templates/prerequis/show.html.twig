{% extends 'back_office/backbase.html.twig' %}

{% block title %}Prerequis{% endblock %}

{% block body %}
    <h1>Prerequis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idprerequis</th>
                <td>{{ prerequi.idprerequis }}</td>
            </tr>
            <tr>
                <th>Nomprerequis</th>
                <td>{{ prerequi.nomprerequis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prerequis_index') }}">back to list</a>

    <a href="{{ path('app_prerequis_edit', {'idprerequis': prerequi.idprerequis}) }}">edit</a>

    {{ include('prerequis/_delete_form.html.twig') }}
{% endblock %}
