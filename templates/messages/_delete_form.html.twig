<form method="post" action="{{ path('app_messages_delete', {'idmessage': message.idmessage}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.idmessage) }}">
    <button class="btn">Delete</button>
</form>
