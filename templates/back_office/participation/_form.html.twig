
{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}

<table>
    <tr>
        <div class="form-group position-relative has-icon-left mb-4">
            {{ form_widget(form.resultat, { attr: { class: 'form-control form-control-xl', placeholder: 'resultat' } }) }}
            <td> {{ form_errors(form.resultat) }}</td>

        </div>
    </tr>
    <tr>
        <div class="form-group position-relative has-icon-left mb-4">
            {{ form_widget(form.user, { attr: { class: 'form-control form-control-xl', placeholder: 'user' } }) }}
            <td> {{ form_errors(form.user) }}</td>

        </div>
    </tr>

    <tr>
        <div class="form-group position-relative has-icon-left mb-4">
            {{ form_widget(form.formation, { attr: { class: 'form-control form-control-xl', placeholder: 'formation' } }) }}
            <td> {{ form_errors(form.formation) }}</td>

        </div>
    </tr>

</table>
<button class="btn btn-primary btn-block btn-lg shadow-lg mt-5">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}