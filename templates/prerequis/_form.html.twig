{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
<table>

    <tr>
        <div class="form-group position-relative has-icon-left mb-4">
            {{ form_widget(form.nomprerequis, { attr: { class: 'form-control form-control-xl', placeholder: 'Nom de Prerequis ' }, size : 5  }) }}
            <td> {{ form_errors(form.nomprerequis) }}</td>

        </div>
    </tr>

</table>
<button class="btn btn-primary btn-block btn-lg shadow-lg mt-5">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}








