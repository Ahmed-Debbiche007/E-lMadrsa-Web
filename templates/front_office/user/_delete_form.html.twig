<form method="post" action="{{ path('app_badge_delete', {'badgeid': badge.badgeid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ badge.badgeid) }}">
    <button class="badge bg-danger">Delete</button>
</form>



